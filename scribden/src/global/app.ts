import '@ionic/core';
// import { setupConfig } from '../helpers/environmentConfig';

export default () => {
  /*
    setupConfig({
      projectURL: '',
      projectID: '',
      key: '',
      secret: ''
    })
  */
};
